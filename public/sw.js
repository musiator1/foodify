// Minimalny Service Worker - to dzięki niemu przeglądarka instaluje aplikację jako PWA
self.addEventListener('fetch', function(event) {
  // Przeglądarka widzi, że nasłuchujemy zapytań i uznaje apkę za PWA
});